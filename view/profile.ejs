<!DOCTYPE html>
<% if (userInfo.opusActive) { %>
<html lang="en" id="opus">
<% } else { %>
<html lang="en">
<% } %>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel='shortcut icon' type='image/x-icon' href='/resources/favicon.ico' />
  <link href="https://fonts.googleapis.com/css2?family=Medula+One&family=Montserrat:wght@400;700&family=Pacifico&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style/global.css">
  <link rel="stylesheet" href="style/profile.css">
  <title><%= title %></title>
</head>
<body>
  <header>
    <a href="#chat" class="disabled"><img src="../resources/chat.svg" alt="Chat Icon">Chat</a>
    <h1>Profile</h1>
    <a href="discover"><img src="../resources/discover.svg" alt="Discover Icon">Discover</a>
</header>
  <main>
    <section class="profile-overview">
      <% if (userInfo.opusActive) { %>
        <% if (userInfo.artwork) { %>
          <img src="uploads/<%= userInfo.artwork.image %>" alt="Your Artwork Image" onclick="location.href='artwork'">
          <h3><a href="set-user"><%= userInfo.userName %></a> - <%= userInfo.artwork.title %></h3>
          <p>- <%= userInfo.artwork.artform %> -</p>
          <% if (userInfo.artwork.ownWork) { %>
            <p>A beautiful artwork created by you!</p>
          <% } else { %>
            <p>Your favorite work of art!</p>
          <% } %>
        <% } else { %>
          <img src="resources/upload-placeholder.png" alt="Your Artwork Image" onclick="location.href='artwork'">
          <p>Upload your favorite artwork</p>
        <% } %>
      <% } else { %>
        <img src="resources/profile-picture.png" alt="Profile Image">
        <h3><a href="set-user"><%= userInfo.userName %></a></h3>
        <p>20</p>
      <% } %>
    </section>
    <section class="opus-settings">
      <h3>Opus</h3>
      <p>Opus is a new feature that allows you to share your own or favourite art instead of selfies, finding matches with similar creative interests.</p>

      <% if (userInfo.opusActive) { %>
        <form action="/profile" method="POST" enctype="multipart/form-data">
          <input class="deactive" type="submit" name="false" value="Deactivate Opus">
        </form>
        <a href="artwork">Upload your art</a>
      <% } else { %>
        <form action="/profile" method="POST" enctype="multipart/form-data">
          <input type="submit" name="true" value="Activate Opus">
        </form>
      <% } %>
    </section>
  </main>
</body>
</html>
