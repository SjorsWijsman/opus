<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link href="https://fonts.googleapis.com/css2?family=Medula+One&family=Montserrat:wght@400;700&family=Pacifico&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style/global.css">
  <link rel="stylesheet" href="style/profile.css">
  <title><%= title %></title>
  <% if (opus.active) { %>
    <style>
      html {
        background-image: url("../resources/background.png");
      }
    </style>
  <% } else { %>
    <style>
      html {
        background-image: url("../resources/background-gray.png");
      }
    </style>
  <% } %>
</head>
<body>
  <header>
    <a href="#chat">Chat</a>
    <h1>Profile</h1>
    <a href="discover">Discover</a>
  </header>
  <main>
    <section class="profile-overview">
      <% if (opus.active) { %>
        <% if (opus.artwork) { %>
          <img src="uploads/upload.png" alt="Your Artwork Image" onclick="location.href='artwork'">
          <h3><%= opus.artwork.title %></h3>
          <p>- <%= opus.artwork.artform %> -</p>
          <% if (opus.artwork.ownWork) { %>
            <p>A beautiful artwork created by you!</p>
          <% } else { %>
            <p>Your favorite work of art!</p>
          <% } %>
        <% } else { %>
          <img src="resources/upload-placeholder.png" alt="Your Artwork Image" onclick="location.href='artwork'">
          <p>Upload your favorite artwork</p>
        <% } %>
      <% } else { %>
        <img src="resources/profile-picture.png" alt="Profile Image">
        <h3>Your Name</h3>
        <p>20</p>
      <% } %>
    </section>
    <section class="opus-settings">
      <h3>Opus</h3>
      <p>Opus is a new feature that allows you to share your own or favourite art instead of selfies, finding matches with similar creative interests.</p>

      <% if (opus.active) { %>
        <form action="/profile" method="POST" enctype="multipart/form-data">
          <button class="deactivate">Deactivate Opus</button>
        </form>
        <a href="artwork">Upload your art</a>
      <% } else { %>
        <form action="/profile" method="POST" enctype="multipart/form-data">
          <button>Activate Opus</button>
        </form>
      <% } %>
    </section>
  </main>
</body>
</html>
